# فحص هيكلية القوائم الجانبية

## ✅ القوائم المعرّفة في DashboardLayout

### القائمة الأساسية (لجميع الأدوار):
1. ✅ لوحة التحكم (`/dashboard`)
2. ✅ التنبيهات (`/dashboard/notifications`)

### قائمة السكرتير (secretary) أو المدير (admin):
3. ✅ المرضى (`/dashboard/patients`)
4. ✅ المواعيد (`/dashboard/appointments`)

### قائمة الطبيب (doctor) أو المدير (admin):
5. ✅ الاستشارات (`/dashboard/consultations`)

### قائمة المختبر (labOwner) أو المدير (admin):
6. ✅ تقارير المختبر (`/dashboard/lab-reports`)

### قائمة المدير فقط (admin):
7. ✅ سجل النشاط (`/dashboard/audit-logs`)
8. ✅ الإعدادات (`/dashboard/settings`)

---

## 📋 الصفحات المتاحة حسب الدور

### دور السكرتير (secretary):
- ✅ لوحة التحكم
- ✅ التنبيهات
- ✅ المرضى (قائمة، جديد، تفاصيل، تعديل)
- ✅ المواعيد (قائمة، جديد، تفاصيل)

### دور الطبيب (doctor):
- ✅ لوحة التحكم
- ✅ التنبيهات
- ✅ الاستشارات (قائمة، جديد، تفاصيل، إرسال)

### دور المختبر (labOwner):
- ✅ لوحة التحكم
- ✅ التنبيهات
- ✅ تقارير المختبر (قائمة، جديد، تفاصيل، تعديل)

### دور المدير (admin):
- ✅ لوحة التحكم
- ✅ التنبيهات
- ✅ المرضى (جميع الصفحات)
- ✅ المواعيد (جميع الصفحات)
- ✅ الاستشارات (جميع الصفحات)
- ✅ تقارير المختبر (جميع الصفحات)
- ✅ سجل النشاط
- ✅ الإعدادات

---

## 🔍 التحقق من الكود

### في `components/Layout/DashboardLayout.tsx`:

```typescript
const getMenuItems = () => {
  const baseItems = [
    { href: '/dashboard', label: 'لوحة التحكم', icon: LayoutDashboard },
    { href: '/dashboard/notifications', label: 'التنبيهات', icon: Bell },
  ];

  if (userRole === 'secretary' || userRole === 'admin') {
    baseItems.push(
      { href: '/dashboard/patients', label: 'المرضى', icon: Users },
      { href: '/dashboard/appointments', label: 'المواعيد', icon: Calendar }
    );
  }

  if (userRole === 'doctor' || userRole === 'admin') {
    baseItems.push(
      { href: '/dashboard/consultations', label: 'الاستشارات', icon: FileText }
    );
  }

  if (userRole === 'labOwner' || userRole === 'admin') {
    baseItems.push(
      { href: '/dashboard/lab-reports', label: 'تقارير المختبر', icon: FileText }
    );
  }

  if (userRole === 'admin') {
    baseItems.push(
      { href: '/dashboard/audit-logs', label: 'سجل النشاط', icon: FileText },
      { href: '/dashboard/settings', label: 'الإعدادات', icon: Settings }
    );
  }

  return baseItems;
};
```

**✅ الكود صحيح ويجب أن يعرض جميع الخيارات حسب الدور**

---

## 🐛 المشاكل المحتملة

### 1. المشكلة: الخيارات لا تظهر
**السبب المحتمل**: `userRole` لا يتم تمريره بشكل صحيح

**الحل**: 
- تأكد من أن جميع الصفحات تمرر `userRole={user.role}`
- تأكد من أن `user.role` يحتوي على القيمة الصحيحة ('doctor', 'secretary', 'labOwner', 'admin')

### 2. المشكلة: خيار Admin لا يظهر في صفحة اختيار الدور
**الحل**: ✅ تم إضافة خيار Admin في `app/select-role/page.tsx`

### 3. المشكلة: القائمة الجانبية لا تتحدث
**السبب المحتمل**: React لا يعيد رسم المكون

**الحل**: 
- تأكد من أن `userRole` يتغير عند تغيير الدور
- تأكد من أن `getMenuItems()` يتم استدعاؤه عند تغيير `userRole`

---

## ✅ التحقق النهائي

1. ✅ جميع الخيارات معرّفة في `getMenuItems()`
2. ✅ جميع الصفحات تمرر `userRole` بشكل صحيح
3. ✅ خيار Admin موجود في صفحة اختيار الدور
4. ✅ الأيقونات معرّفة بشكل صحيح
5. ✅ المسارات (hrefs) صحيحة

---

## 🚀 الخطوات التالية

إذا كانت الخيارات لا تظهر:

1. **افتح Developer Console** في المتصفح
2. **تحقق من قيمة `userRole`** في localStorage:
   ```javascript
   JSON.parse(localStorage.getItem('user')).role
   ```
3. **تحقق من أن القائمة الجانبية تعيد الرسم** عند تغيير الدور
4. **امسح localStorage** وجرب مرة أخرى:
   ```javascript
   localStorage.clear()
   ```

---

**آخر تحديث**: 2025-12-01

